/**
 * Certification — interval tests that promote an approximate root to a theorem.
 */
export { krawczyk, certifiedRadiusWindow, certifiesPoint, imageInsideBox } from './krawczyk';
export type {
  IntervalSystem,
  KrawczykResult,
  KrawczykVerdict,
  RadiusWindow,
  RadiusWindowOptions,
} from './krawczyk';
