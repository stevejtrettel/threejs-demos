/**
 * Shared types for the math library
 *
 * Re-exports from main types file for backwards compatibility.
 * New code should import directly from '@/types'.
 */

export type {
  Parametric,
  Rebuildable,
  Updatable,
  ParamTrigger
} from '@/types';
